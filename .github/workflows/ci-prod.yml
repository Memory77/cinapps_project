name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Create .env file for Docker Compose
        run: |
          echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
          echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env
          echo "MYSQL_HOST=${{ secrets.MYSQL_HOST }}" >> .env
          echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
          echo "API_CRUD_USERNAME=${{ secrets.API_CRUD_USERNAME }}" >> .env
          echo "API_CRUD_PASSWORD=${{ secrets.API_CRUD_PASSWORD }}" >> .env
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> .env
          echo "ALGORITHM=${{ secrets.ALGORITHM }}" >> .env
          echo "ACCESS_TOKEN_EXPIRE_MINUTES=${{ secrets.ACCESS_TOKEN_EXPIRE_MINUTES }}" >> .env
          echo "URL_API=${{ secrets.URL_API }}" >> .env
          # Ajoute ici toutes les autres variables d’environnement nécessaires

      - name: Build and start containers
        run: docker compose -f docker-compose.prod.yml up -d --build

      - name: Wait for services to be ready
        run: sleep 15

      - name: Run API integration tests
        run: pytest API_s/test_api_integration.py

      - name: Run Django tests
        run: pytest cinapps_api/tests/

      - name: Stop containers
        run: docker compose down

      - name: Remove .env file
        run: rm .env
